<template>
  <div v-if="exibir" class="modal-overlay" @click.self="$emit('fechar')">
    <div class="modal-card">
      <header class="modal-header">
        <h2 class="modal-title">
          <slot name="titulo">Título do Modal</slot>
        </h2>
        <button @click="$emit('fechar')" class="btn-fechar">×</button>
      </header>
      <main class="modal-body">
        <slot />
      </main>
    </div>
  </div>
</template>

<script setup>
defineProps({
  exibir: {
    type: Boolean,
    default: false,
  },
});
defineEmits(["fechar"]);
</script>

<style scoped>
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.6);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}
.modal-card {
  background: white;
  padding: 24px;
  border-radius: 8px;
  box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
  width: 90%;
  max-width: 500px;
}
.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid #eee;
  padding-bottom: 1rem;
  margin-bottom: 1rem;
}
.modal-title {
  margin: 0;
  font-size: 1.5rem;
}
.btn-fechar {
  background: none;
  border: none;
  font-size: 2.5rem;
  line-height: 1;
  cursor: pointer;
  color: #888;
}
</style>
